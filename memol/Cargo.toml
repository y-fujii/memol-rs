[package]
name = "memol"
edition = "2024"
build = "build.rs"

[dependencies]
lalrpop-util = { version = "*", features = ["lexer"] }
regex = "*"

[build-dependencies]
lalrpop = { version = "*", features = ["lexer"] }
