[package]
name = "memol_util"
edition = "2024"

[dependencies]
libloading = { version = "*", default-features = false }
memol = { path = "../memol" }

[target.'cfg(target_family = "windows")'.dependencies]
windows = { version = "*", default-features = false, features = ["Win32_Media_Audio"] }
